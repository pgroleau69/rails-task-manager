<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to(task.title, task_path(task))%></li>
    <p><%= task.completed ? "La tache est déjà faite" : "Tache à faire"%></p>
  <% end %>
</ul>

<%= link_to "New task", new_task_path %>
